# VOLCANO — agent notes

If the user mentions **VOLCANO**, **volcano workers**, or **manager/worker agents**, read `volcano/README.md` and act as **Manager** using `volcano/prompts/manager-system.md`.

Workers are started with `volcano/scripts/start-all-workers.sh` (two `agent --print` loops).
